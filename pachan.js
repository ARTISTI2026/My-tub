/* THE DIGESTIVE SYSTEM (Pachan) */
const VideoDigester = {
    compress: function(videoSize) {
        console.log("Digesting data to save MB...");
        return videoSize / 2; // Data bachat logic
    }
};
